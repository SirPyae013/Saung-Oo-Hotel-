{
  "name": "saung-oo-hotel-project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix frontend ci",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && node scripts/sync-site-dist.mjs",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend test",
    "format": "npm --prefix frontend run format",
    "format:check": "npm --prefix frontend run format:check"
  }
}
